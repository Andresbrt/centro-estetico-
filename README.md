# Centro de Experiencia Afro — Scaffold

Proyecto inicial con React + Vite + TypeScript y Supabase.

Para arrancar:

```bash
npm install
npm run dev
```

Copiar `.env.example` a `.env` y rellenar las variables:

```
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
```
